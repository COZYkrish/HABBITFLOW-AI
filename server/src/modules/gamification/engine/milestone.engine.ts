export const evaluateMilestones = async (userId: string, event: any): Promise<string[]> => {
  // Logic for grand milestones
  // e.g., 100 Habits Completed, 500 Habits Completed, 365 Active Days.
  // This could query the HabitLog collection directly to count totals.
  // For now, we'll return an empty array and leave it ready for expansion.
  return [];
};
